<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aj.android.messenger">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Messenger">
        <activity android:name=".Activity.SignUpActivty"
            android:theme="@style/Theme.Messenger.NoActionBar"/>
        <activity android:name=".Activity.Splash"
            android:theme="@style/Theme.Messenger.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity.SingleChatActivity"
            android:theme="@style/Theme.Messenger.NoActionBar" />
        <activity
            android:name=".Activity.HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Messenger.NoActionBar" />
    </application>

</manifest>